[build]
  command = "mkdir -p dist && cp index.html styles.css script.js portfolio-data.js Atul_Pandey_CV.pdf dist/ && cp -r images dist/ && cp -r unreleased-projects dist/ && cp -r work-experience dist/"
  publish = "dist"
